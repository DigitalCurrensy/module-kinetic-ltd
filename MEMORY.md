# Status

Updated 2026-09-24.

- Seven houses are on this parent. Tests run on every push and every Monday.
- A house outbox is in memory until a host sets `DATABASE_URL`. An unset URL writes nothing.
- The signing key is a host secret. It is not a column and not a file in git.
- Authored readings are not a charger, a cabinet, a switch, or a span.
- This repository is not a charger-management server and not a hardware module.
